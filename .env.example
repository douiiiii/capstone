# PostgreSQL 연결 정보 (미설정 시 SQLite 사용)
DATABASE_URL=postgresql://user:password@localhost:5432/hwaseong_hub

# Flask 설정
SECRET_KEY=your-secret-key
FLASK_ENV=development
PORT=5000

# 관리자 전용 API 인증 토큰 (X-Admin-Token 헤더)
# 운영 환경에서는 강력한 무작위 문자열로 변경할 것
ADMIN_TOKEN=change-me-in-production
